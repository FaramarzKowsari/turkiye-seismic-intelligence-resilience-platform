# Data directory

`data/demo/` contains deterministic synthetic datasets generated by
`scripts/generate_demo.py`. They are not official observations and must not be used for hazard or
operational conclusions.

Place private or licensed local data in `local_data/`; that directory is ignored by Git.
