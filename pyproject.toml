[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "depremnabiz-ai"
version = "1.0.0"
description = "Open-source earthquake analytics, satellite deformation readiness and urban exposure intelligence for Türkiye."
readme = "README.md"
requires-python = ">=3.11"
license = {text = "MIT"}
authors = [{name = "Faramarz Kowsari"}]
keywords = [
  "earthquake analytics",
  "Türkiye",
  "seismology",
  "GeoAI",
  "disaster resilience",
  "Sentinel-1",
  "urban exposure",
  "Streamlit",
  "FastAPI"
]
dependencies = [
  "numpy>=2.0",
  "pandas>=2.2",
  "requests>=2.32",
  "pydantic>=2.8",
  "scikit-learn>=1.5",
  "plotly>=5.23",
  "openpyxl>=3.1",
  "pyarrow>=17.0",
  "typer>=0.12",
  "fastapi>=0.115",
  "uvicorn>=0.30"
]

[project.optional-dependencies]
app = ["streamlit>=1.45"]
dev = [
  "pytest>=8.3",
  "pytest-cov>=5.0",
  "ruff>=0.8",
  "mypy>=1.13",
  "httpx>=0.27",
  "PyYAML>=6.0.2"
]

[project.scripts]
depremnabiz = "depremnabiz.cli:app"

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"

[tool.ruff]
line-length = 130
target-version = "py311"
extend-exclude = ["notebooks"]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
